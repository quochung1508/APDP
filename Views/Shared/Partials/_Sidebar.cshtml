<div id="layoutSidenav_nav">
    <nav class="sb-sidenav accordion sb-sidenav-light bg-info.bg-gradient" id="sidenavAccordion">
        <div class="sb-sidenav-menu">
            <div class="nav">
                <a class="nav-link text-border text-black" href="@Url.Action("Index", "Home")">
                    <div class="sb-nav-link-icon"><i class="fas fa-tachometer-alt"></i></div>
                    Dashboard
                </a>

                @if (User.IsInRole("Admin"))
                {
                    <div class="sb-sidenav-menu-heading">Management</div>

                    <a class="nav-link" href="@Url.Action("Manage", "ClassAssignments")">
                        <div class="sb-nav-link-icon"><i class="fas fa-tasks"></i></div>
                        Assignments
                    </a>

                    <!-- Accounts Management -->
                    <a class="nav-link collapsed" href="#" data-bs-toggle="collapse" data-bs-target="#collapseAccounts" aria-expanded="false" aria-controls="collapseAccounts">
                        <div class="sb-nav-link-icon"><i class="fas fa-users-cog"></i></div>
                        Accounts
                        <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>
                    </a>
                    <div class="collapse" id="collapseAccounts" aria-labelledby="headingAccounts" data-bs-parent="#sidenavAccordion">
                        <nav class="sb-sidenav-menu-nested nav">
                            <a class="nav-link" href="@Url.Action("ManageUsers", "Accounts")">All Users</a>
                            <a class="nav-link" href="@Url.Action("CreateStudent", "Accounts")">Add Student</a>
                            <a class="nav-link" href="@Url.Action("CreateTeacher", "Accounts")">Add Teacher</a>
                        </nav>
                    </div>

                    <!-- Academic Management -->
                    <a class="nav-link collapsed" href="#" data-bs-toggle="collapse" data-bs-target="#collapseAcademic" aria-expanded="false" aria-controls="collapseAcademic">
                        <div class="sb-nav-link-icon"><i class="fas fa-book-reader"></i></div>
                        Academic
                        <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>
                    </a>
                    <div class="collapse" id="collapseAcademic" aria-labelledby="headingAcademic" data-bs-parent="#sidenavAccordion">
                        <nav class="sb-sidenav-menu-nested nav">
                            <a class="nav-link" href="@Url.Action("Index", "Class")">Classes</a>
                            <a class="nav-link" href="@Url.Action("Index", "Course")">Courses</a>
                        </nav>
                    </div>

                    <!-- Student Management -->
                    <a class="nav-link" href="@Url.Action("Index", "Student")">
                        <div class="sb-nav-link-icon"><i class="fas fa-user-graduate"></i></div>
                        Students
                    </a>

                    <!-- Teacher Management -->
                    <a class="nav-link" href="@Url.Action("ManageUsers", "Accounts", new { role = "Teacher" })">
                        <div class="sb-nav-link-icon"><i class="fas fa-chalkboard-teacher"></i></div>
                        Teachers
                    </a>
                }

                @if (User.IsInRole("Student"))
                {
                    <div class="sb-sidenav-menu-heading">Student Menu</div>
                    
                    <a class="nav-link" href="@Url.Action("Schedule", "Student")">
                        <div class="sb-nav-link-icon"><i class="fas fa-calendar-alt"></i></div>
                        Lịch học (Schedule)
                    </a>
                    
                    <a class="nav-link" href="@Url.Action("AttendanceReport", "Student")">
                        <div class="sb-nav-link-icon"><i class="fas fa-chart-pie"></i></div>
                        Báo cáo Điểm danh
                    </a>
                }

                @if (User.IsInRole("Teacher"))
                {
                    <div class="sb-sidenav-menu-heading">Teacher Menu</div>
                    <a class="nav-link" href="@Url.Action("Schedule", "Teacher")">
                        <div class="sb-nav-link-icon"><i class="fas fa-calendar-alt"></i></div>
                        View Teaching Schedule
                    </a>
                    <a class="nav-link" href="@Url.Action("Index", "Attendance")">
                        <div class="sb-nav-link-icon"><i class="fas fa-clipboard-check"></i></div>
                        Attendance
                    </a>

                }

            </div>
        </div>
        <div class="sb-sidenav-footer">
            <div class="small">Logged in as:</div>
            @if (User.Identity.IsAuthenticated)
            {
                @User.Identity.Name
            }
        </div>
    </nav>
</div>
